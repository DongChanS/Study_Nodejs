// TODO : 매개변수를 받아서 최솟값을 반환
var _ = require('lodash')

function getMin(a, b){
    if (a > b){
        return b
    } 
    return a
}

function getMin2(a, b){
    var min = Infinity
    if (a > b){
        min = b
    } else {
        min = a
    }
    return min
}

var getMinFromArray = function(numbers){
    var min = Infinity
    // for (let i=0; i<numbers.length; i++){
    //     if (numbers[i] < min) {
    //         min = numbers[i]
    //     }
    // }

    // 파이썬은 in인데 헷갈리지말것!
    for (num of numbers){
        // if (min > num) {
        //     min = num
        // }
        min = (min > num) ? num : min;
    }
    return min
}

let arr = [1,2,3,4,5];

console.log(getMinFromArray(arr));

console.log(typeof 1);